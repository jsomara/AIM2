<% provide(:title, 'Projects') %>

<h1>All projects</h1>

<%= link_to "New Project", 'projects/new', class: "btn btn-large btn-primary" %>

<ul class="projects">
  <% @projects.each do |p| %>
    <p>
    <%= link_to p.title, project_path(p) %>
  <% end %>
</ul>
